# Fake BUCK module to cause IntelliJ to show directory structure instead of module lists.

java_library(
  name = 'fake',
  srcs = glob(['*.java']),
  deps = [
  ],
)

project_config(
  src_target = ':fake',
)

